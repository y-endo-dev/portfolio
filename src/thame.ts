import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: "Noto Sans, Arial, sans-serif", // Noto Sans をデフォルトフォントとして指定
  },
  // その他のテーマ設定
});

export default theme;
